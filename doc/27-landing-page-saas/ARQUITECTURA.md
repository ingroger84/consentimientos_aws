# Arquitectura - Landing Page SaaS

**Fecha:** 2026-01-21

---

## 🏗️ Diagrama de Arquitectura

```
┌─────────────────────────────────────────────────────────────────┐
│                         INTERNET                                 │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      DNS (Route 53)                              │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  datagree.net          → IP_SERVIDOR                      │  │
│  │  *.datagree.net        → IP_SERVIDOR (wildcard)           │  │
│  │  www.datagree.net      → datagree.net (CNAME)             │  │
│  │  api.datagree.net      → IP_SERVIDOR                      │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    AWS LIGHTSAIL SERVER                          │
│                                                                   │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │                    NGINX (Port 80/443)                      │ │
│  │  ┌──────────────────────────────────────────────────────┐  │ │
│  │  │  SSL/TLS (Let's Encrypt)                             │  │ │
│  │  │  - datagree.net                                       │  │ │
│  │  │  - *.datagree.net (wildcard)                          │  │ │
│  │  └──────────────────────────────────────────────────────┘  │ │
│  │                                                              │ │
│  │  ┌──────────────────┐  ┌──────────────────┐               │ │
│  │  │  Landing Page    │  │  Tenant Apps     │               │ │ │
│  │  │  datagree.net    │  │  *.datagree.net  │               │ │ │
│  │  │  Port: 80/443    │  │  Port: 80/443    │               │ │ │
│  │  └──────────────────┘  └──────────────────┘               │ │
│  └────────────────────────────────────────────────────────────┘ │
│                              │                                    │
│                              ▼                                    │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │              FRONTEND (React + Vite)                        │ │
│  │  ┌──────────────────────────────────────────────────────┐  │ │
│  │  │  /dist/                                               │  │ │
│  │  │  - index.html                                         │  │ │
│  │  │  - assets/                                            │  │ │
│  │  │  - PublicLandingPage                                  │  │ │
│  │  │  - PricingSection                                     │  │ │
│  │  │  - SignupModal                                        │  │ │
│  │  └──────────────────────────────────────────────────────┘  │ │
│  └────────────────────────────────────────────────────────────┘ │
│                              │                                    │
│                              ▼                                    │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │              BACKEND (NestJS + PM2)                         │ │
│  │  ┌──────────────────────────────────────────────────────┐  │ │
│  │  │  Port: 3000                                           │  │ │
│  │  │                                                        │  │ │
│  │  │  Endpoints:                                           │  │ │
│  │  │  - GET  /tenants/plans (público)                     │  │ │
│  │  │  - POST /tenants (público)                           │  │ │
│  │  │  - POST /auth/login                                   │  │ │
│  │  │  - ...otros endpoints                                 │  │ │
│  │  └──────────────────────────────────────────────────────┘  │ │
│  └────────────────────────────────────────────────────────────┘ │
│                              │                                    │
│                              ▼                                    │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │              PostgreSQL (Port 5432)                         │ │
│  │  ┌──────────────────────────────────────────────────────┐  │ │
│  │  │  Database: consentimientos                            │  │ │
│  │  │                                                        │  │ │
│  │  │  Tables:                                              │  │ │
│  │  │  - tenants                                            │  │ │
│  │  │  - users                                              │  │ │
│  │  │  - roles                                              │  │ │
│  │  │  - settings                                           │  │ │
│  │  │  - ...otras tablas                                    │  │ │
│  │  └──────────────────────────────────────────────────────┘  │ │
│  └────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    SERVICIOS EXTERNOS                            │
│  ┌──────────────────┐  ┌──────────────────┐  ┌───────────────┐ │
│  │   AWS S3         │  │   SMTP (Gmail)   │  │  Bold API     │ │
│  │   Almacenamiento │  │   Correos        │  │  Pagos        │ │
│  └──────────────────┘  └──────────────────┘  └───────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Flujo de Registro de Usuario

```
┌──────────────┐
│   Usuario    │
└──────┬───────┘
       │
       │ 1. Visita datagree.net
       ▼
┌──────────────────────────────┐
│   Landing Page               │
│   (PublicLandingPage.tsx)    │
└──────┬───────────────────────┘
       │
       │ 2. Navega y selecciona plan
       ▼
┌──────────────────────────────┐
│   Pricing Section            │
│   (PricingSection.tsx)       │
└──────┬───────────────────────┘
       │
       │ 3. Click "Seleccionar Plan"
       ▼
┌──────────────────────────────┐
│   Signup Modal               │
│   (SignupModal.tsx)          │
└──────┬───────────────────────┘
       │
       │ 4. Llena formulario
       │ 5. Click "Crear Cuenta"
       ▼
┌──────────────────────────────┐
│   POST /tenants              │
│   (Backend API)              │
└──────┬───────────────────────┘
       │
       │ 6. Validaciones
       ▼
┌──────────────────────────────┐
│   Tenants Service            │
│   (tenants.service.ts)       │
└──────┬───────────────────────┘
       │
       │ 7. Crear tenant en DB
       │ 8. Crear usuario admin
       │ 9. Inicializar settings
       ▼
┌──────────────────────────────┐
│   PostgreSQL                 │
│   (Base de Datos)            │
└──────┬───────────────────────┘
       │
       │ 10. Tenant y usuario creados
       ▼
┌──────────────────────────────┐
│   Mail Service               │
│   (mail.service.ts)          │
└──────┬───────────────────────┘
       │
       │ 11. Enviar correo de bienvenida
       ▼
┌──────────────────────────────┐
│   SMTP (Gmail)               │
└──────┬───────────────────────┘
       │
       │ 12. Correo enviado
       ▼
┌──────────────────────────────┐
│   Usuario recibe correo      │
│   con credenciales           │
└──────┬───────────────────────┘
       │
       │ 13. Accede a [slug].datagree.net/login
       ▼
┌──────────────────────────────┐
│   Login Page                 │
└──────┬───────────────────────┘
       │
       │ 14. Ingresa credenciales
       ▼
┌──────────────────────────────┐
│   POST /auth/login           │
└──────┬───────────────────────┘
       │
       │ 15. JWT Token generado
       ▼
┌──────────────────────────────┐
│   Dashboard                  │
│   ¡Usuario autenticado!      │
└──────────────────────────────┘
```

---

## 📦 Componentes Frontend

```
frontend/src/
│
├── pages/
│   ├── PublicLandingPage.tsx ─────┐
│   │   ├── Hero Section           │
│   │   ├── Stats Section          │
│   │   ├── Features Section       │
│   │   ├── Benefits Section       │
│   │   ├── Use Cases Section      │
│   │   ├── Testimonials Section   │
│   │   ├── Pricing Section ───────┼──┐
│   │   ├── CTA Section            │  │
│   │   └── Footer                 │  │
│   │                               │  │
│   ├── LoginPage.tsx              │  │
│   ├── DashboardPage.tsx          │  │
│   └── ...otras páginas           │  │
│                                   │  │
├── components/                     │  │
│   ├── landing/                    │  │
│   │   ├── PricingSection.tsx ◄───┘  │
│   │   │   ├── Carga planes          │
│   │   │   ├── Toggle mensual/anual │
│   │   │   ├── Cards de planes      │
│   │   │   └── Botón seleccionar ───┼──┐
│   │   │                             │  │
│   │   └── SignupModal.tsx ◄─────────┘  │
│   │       ├── Formulario empresa      │
│   │       ├── Formulario admin        │
│   │       ├── Validaciones            │
│   │       ├── Estado success          │
│   │       └── Estado error            │
│   │                                    │
│   ├── Layout.tsx                      │
│   ├── PrivateRoute.tsx                │
│   └── ...otros componentes            │
│                                        │
├── services/                            │
│   ├── api.ts ◄─────────────────────────┘
│   ├── auth.service.ts
│   └── ...otros servicios
│
├── store/
│   ├── authStore.ts
│   └── ...otros stores
│
└── App.tsx
    └── Router
        ├── / → PublicLandingPage
        ├── /login → LoginPage
        └── /dashboard → DashboardPage (privado)
```

---

## 🔌 Endpoints Backend

```
backend/src/
│
├── tenants/
│   ├── tenants.controller.ts
│   │   ├── GET  /tenants/plans (público) ──────┐
│   │   ├── POST /tenants (público) ────────────┼──┐
│   │   ├── GET  /tenants (privado)             │  │
│   │   └── ...otros endpoints                  │  │
│   │                                            │  │
│   ├── tenants.service.ts ◄────────────────────┘  │
│   │   ├── create() ◄──────────────────────────────┘
│   │   │   ├── Validar datos
│   │   │   ├── Generar slug
│   │   │   ├── Verificar unicidad
│   │   │   ├── Crear tenant
│   │   │   ├── Crear usuario admin
│   │   │   ├── Inicializar settings
│   │   │   └── Enviar correo ──────────┐
│   │   │                                │
│   │   ├── findAll()                    │
│   │   ├── findOne()                    │
│   │   └── ...otros métodos             │
│   │                                     │
│   ├── plans.config.ts                  │
│   │   └── PLANS (5 planes)             │
│   │                                     │
│   └── dto/                              │
│       └── create-tenant.dto.ts         │
│                                         │
├── mail/                                 │
│   ├── mail.service.ts ◄─────────────────┘
│   │   ├── sendWelcomeEmail()
│   │   │   ├── Generar HTML
│   │   │   ├── Incluir credenciales
│   │   │   └── Enviar vía SMTP
│   │   │
│   │   ├── sendPasswordResetEmail()
│   │   └── sendConsentEmail()
│   │
│   └── templates/
│       └── welcome-email.html
│
├── auth/
│   ├── auth.controller.ts
│   │   ├── POST /auth/login
│   │   ├── POST /auth/forgot-password
│   │   └── POST /auth/reset-password
│   │
│   └── auth.service.ts
│       ├── login()
│       ├── validateUser()
│       └── validateTenantAccess()
│
└── ...otros módulos
```

---

## 🗄️ Modelo de Datos

```sql
-- Tabla: tenants
CREATE TABLE tenants (
    id UUID PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    slug VARCHAR(100) UNIQUE NOT NULL,
    contact_name VARCHAR(255),
    contact_email VARCHAR(255),
    contact_phone VARCHAR(50),
    plan VARCHAR(50) NOT NULL,
    plan_price DECIMAL(10,2),
    billing_cycle VARCHAR(20),
    status VARCHAR(20) NOT NULL,
    max_users INTEGER,
    max_branches INTEGER,
    max_consents INTEGER,
    max_services INTEGER,
    max_questions INTEGER,
    storage_limit_mb INTEGER,
    trial_ends_at TIMESTAMP,
    subscription_ends_at TIMESTAMP,
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW(),
    deleted_at TIMESTAMP
);

-- Tabla: users
CREATE TABLE users (
    id UUID PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    email VARCHAR(255) UNIQUE NOT NULL,
    password VARCHAR(255) NOT NULL,
    tenant_id UUID REFERENCES tenants(id),
    role_id UUID REFERENCES roles(id),
    reset_password_token VARCHAR(255),
    reset_password_expires TIMESTAMP,
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW(),
    deleted_at TIMESTAMP
);

-- Tabla: roles
CREATE TABLE roles (
    id UUID PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    type VARCHAR(50) NOT NULL,
    permissions JSONB,
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW()
);

-- Tabla: settings
CREATE TABLE settings (
    id UUID PRIMARY KEY,
    tenant_id UUID REFERENCES tenants(id),
    company_name VARCHAR(255),
    company_email VARCHAR(255),
    company_phone VARCHAR(50),
    logo_url VARCHAR(500),
    primary_color VARCHAR(7),
    secondary_color VARCHAR(7),
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW()
);
```

---

## 🔐 Seguridad

### Autenticación:
```
┌──────────────┐
│   Usuario    │
└──────┬───────┘
       │
       │ 1. POST /auth/login
       │    { email, password }
       ▼
┌──────────────────────────────┐
│   Auth Service               │
│   - Validar credenciales     │
│   - Validar tenant           │
│   - Generar JWT              │
└──────┬───────────────────────┘
       │
       │ 2. JWT Token
       │    { userId, tenantId, role }
       ▼
┌──────────────────────────────┐
│   Cliente                    │
│   - Guardar token            │
│   - Incluir en headers       │
└──────┬───────────────────────┘
       │
       │ 3. Requests con token
       │    Authorization: Bearer <token>
       ▼
┌──────────────────────────────┐
│   JWT Guard                  │
│   - Verificar token          │
│   - Extraer payload          │
└──────┬───────────────────────┘
       │
       │ 4. Usuario autenticado
       ▼
┌──────────────────────────────┐
│   Permissions Guard          │
│   - Verificar permisos       │
│   - Permitir/Denegar         │
└──────────────────────────────┘
```

### Validaciones:
```
Frontend:
├── Campos requeridos
├── Formato de email
├── Longitud de contraseña
├── Coincidencia de contraseñas
└── Formato de slug

Backend:
├── DTOs con class-validator
├── Slug único en DB
├── Email único en DB
├── Contraseña hasheada (bcrypt)
└── Validación de plan existente
```

---

## 📧 Flujo de Correos

```
┌──────────────────────────────┐
│   Evento: Tenant creado      │
└──────┬───────────────────────┘
       │
       │ 1. Trigger
       ▼
┌──────────────────────────────┐
│   Mail Service               │
│   sendWelcomeEmail()         │
└──────┬───────────────────────┘
       │
       │ 2. Generar HTML
       ▼
┌──────────────────────────────┐
│   Template HTML              │
│   - Header con gradiente     │
│   - Datos del usuario        │
│   - Credenciales             │
│   - URL de acceso            │
│   - Footer                   │
└──────┬───────────────────────┘
       │
       │ 3. Enviar vía SMTP
       ▼
┌──────────────────────────────┐
│   SMTP Server (Gmail)        │
│   - Autenticación            │
│   - Envío                    │
└──────┬───────────────────────┘
       │
       │ 4. Correo enviado
       ▼
┌──────────────────────────────┐
│   Bandeja del usuario        │
│   ✉️ Correo de bienvenida    │
└──────────────────────────────┘
```

---

## 🌐 Routing Multi-Tenant

```
DNS Resolution:
├── datagree.net
│   └── Landing Page (PublicLandingPage)
│
├── www.datagree.net
│   └── Landing Page (redirect)
│
├── admin.datagree.net
│   └── Super Admin Login
│
├── cliente1.datagree.net
│   └── Tenant App (cliente1)
│
├── cliente2.datagree.net
│   └── Tenant App (cliente2)
│
└── *.datagree.net
    └── Tenant App (cualquier slug)

Backend Validation:
├── Extract subdomain from Host header
├── If no subdomain → Super Admin
├── If subdomain → Find tenant by slug
├── Validate tenant status (active/trial)
└── Allow/Deny access
```

---

## 📊 Métricas y Monitoreo

```
┌──────────────────────────────┐
│   Nginx Access Logs          │
│   - Requests por segundo     │
│   - Códigos de respuesta     │
│   - Tiempos de respuesta     │
└──────────────────────────────┘

┌──────────────────────────────┐
│   PM2 Monitoring             │
│   - CPU usage                │
│   - Memory usage             │
│   - Restart count            │
└──────────────────────────────┘

┌──────────────────────────────┐
│   PostgreSQL Stats           │
│   - Queries por segundo      │
│   - Conexiones activas       │
│   - Tamaño de DB             │
└──────────────────────────────┘

┌──────────────────────────────┐
│   Application Logs           │
│   - Errores                  │
│   - Warnings                 │
│   - Info                     │
└──────────────────────────────┘
```

---

## 🔄 Ciclo de Vida del Tenant

```
1. REGISTRO
   ├── Usuario llena formulario
   ├── Backend crea tenant
   ├── Estado: TRIAL (30 días)
   └── Correo de bienvenida

2. TRIAL (30 días)
   ├── Acceso completo
   ├── Límites según plan
   └── Notificaciones de expiración

3. ACTIVACIÓN
   ├── Pago procesado
   ├── Estado: ACTIVE
   └── Facturación recurrente

4. RENOVACIÓN
   ├── Factura generada automáticamente
   ├── Pago procesado
   └── Extensión de suscripción

5. SUSPENSIÓN (opcional)
   ├── Pago fallido
   ├── Estado: SUSPENDED
   └── Acceso bloqueado

6. REACTIVACIÓN
   ├── Pago procesado
   ├── Estado: ACTIVE
   └── Acceso restaurado

7. CANCELACIÓN
   ├── Usuario solicita
   ├── Estado: EXPIRED
   └── Soft delete después de 30 días
```

---

**Última actualización:** 2026-01-21  
**Versión:** 1.1.28

**Desarrollado con ❤️ por Innova Systems**
